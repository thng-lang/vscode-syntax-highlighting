assn <int> x: 10;
assn <int> y: 20.12091029;

<int>
fn test(u8 x, int y) {
    x + y;
}

assn <u8> value: test(x, y);

action(a, b);

actionpair();

value: 10;

if x = x and !false {}

def Type {
    property: Pairs;
    name: string;

    apply {
        <int> fn test() {
            assign property: 10;
            property += 10;
            return property;
        }
    }
}



assn <Type> x: { 255, "this is a name" };

print("test");